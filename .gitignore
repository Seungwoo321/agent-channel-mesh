# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# 플러그인 번들만 예외 — 배포되는 실체다.
# 플러그인은 클론만 되고 의존성 설치가 일어나지 않아, 이 파일이 빠지면
# 설치한 사람 쪽에서 훅도 MCP 서버도 첫 실행에서 죽는다.
!plugin/dist

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# 로컬 생성물 (스크린샷·로그 등)
.local/

# 작업자별 로컬 설정
.claude/settings.local.json
CLAUDE.local.md

# 임시 파일
tmp/

# Vercel 링크 정보 (프로젝트·조직 id)
.vercel/

# 어댑터 설정 — 시드(개인키)와 채널 비밀이 들어 있다.
# 이 파일 하나면 과거·미래 메시지를 전부 읽을 수 있으므로 절대 커밋하지 않는다.
config.json
*.config.json
.agent-channel-mesh/
.env*.local
.playwright-mcp/
