# agent-channel-mesh

에이전트와 사람이 같은 메시에서 대화한다. 릴레이는 암호문만 지나보내고, 평문은 참여자 로컬에만 존재한다.

> 초기 개발 중. 아직 사용할 수 없다.

## 무엇인가

코딩 에이전트 세션은 서로 격리돼 있다. Claude Code 의 공식 세션 간 메시징조차 같은 OS 사용자의 세션끼리만 닿는다. 다른 사람의 에이전트, 또는 다른 종류의 에이전트와 대화하려면 별도 경로가 필요하다.

이 프로젝트는 그 경로를 만든다. 세 가지를 하나의 전송로로 처리한다.

- **사람 ↔ 에이전트** — 팀원이 내 에이전트에게 지시하거나 결과를 받는다
- **에이전트 ↔ 에이전트** — 각자의 에이전트가 서로 협업한다
- **외부 → 에이전트** — CI·모니터링·웹훅이 세션에 도달한다

## 특징

- **종단 간 암호화** — 릴레이가 내용을 읽지 못한다. 개인키는 로컬 코어 밖으로 나가지 않는다.
- **서버리스 릴레이** — 상시 구동 서버가 필요 없다. 서버리스 함수로 배포한다.
- **에이전트 무관** — 코어에 특정 에이전트 고유 로직이 없다. 어댑터만 갈아 끼운다.
- **발화 제어** — 에이전트끼리 목적 없이 무한 왕복하는 것을 프로토콜이 막는다.

## 구조

```
[에이전트 세션] ──stdio──> [어댑터] [메시 코어] ──HTTPS──> [릴레이] <──── [다른 브릿지]
      평문              에이전트별   암복호화·정책        암호문만
```

암호·정책·릴레이는 어느 에이전트를 쓰든 같은 코어를 지난다. 에이전트별로 다른 것은 **세션에 메시지를 어떻게 넣느냐**뿐이다.

| 에이전트 | 보내기 | 받기 |
|---|---|---|
| Claude Code | MCP 툴 | **채널 주입** — 프롬프트 없이 도착 |
| Codex · 그 외 | MCP 툴 | **수신함 폴링** — 에이전트가 꺼내감 |

Codex 에도 주입 메서드가 있지만(`app-server` 의 `turn/steer`) 진행 중인 턴에만 꽂히고, 세션을 우리가 소유해야 하며, 아직 실험적이다. 즉시성을 얻는 대가로 세션 소유권을 내주는 거래라 v1 은 수신함을 택한다 — 즉시성만 잃고 보안 속성은 그대로다. 근거는 [docs/architecture.md §4](docs/architecture.md) 참조.

자세한 설계는 [docs/architecture.md](docs/architecture.md) 참조.

## 붙이기

어댑터는 에이전트가 띄우는 stdio MCP 서버다. 설정 파일 하나를 읽는다 — 대화창이 없어서 시드도 채널 비밀도 물어볼 수 없다.

### 1. 신원을 만든다

대화에 참여하는 **사람마다 각자의 기계에서** 한 번 실행한다.

```bash
bun run src/adapter/bin.ts init --relay https://relay.example --label alice
```

시드를 만들고 `~/.agent-channel-mesh/config.json` 을 `0600` 으로 쓴다. 그리고 두 가지를 출력한다 — 상대에게 보낼 공개키 블록과, 내 지문이다.

**이미 파일이 있으면 아무것도 하지 않는다.** 시드를 덮어쓰면 신원이 사라지고, 상대가 대조해 둔 지문이 전부 무효가 된다.

### 2. 공개키를 교환하고 지문을 대조한다

`init` 이 출력한 `members` 블록을 상대에게 보내고, 상대 것을 받아 내 `config.json` 의 `channels[].members` 에 넣는다. 나중에 다시 보려면 `whoami` 를 쓴다.

```bash
bun run src/adapter/bin.ts whoami --label alice
```

**지문은 반드시 다른 경로로 대조한다** — 음성 통화나 대면이다. 공개키가 온 그 경로로 온 지문은 아무것도 검증하지 못한다. 값이 어긋나면 중간자가 있다는 뜻이고, 그때 대화를 시작하면 안 된다.

### 3. 채널 비밀을 나눈다

채널을 여는 사람이 `init` 이 출력한 채널 비밀을 참여자에게 전달한다. 같은 비밀을 넣은 노드끼리 같은 채널에 붙는다.

```jsonc
// ~/.agent-channel-mesh/config.json
{
  "seed": "…",                       // init 이 만든다. 이것만 있으면 신원이 재파생된다
  "relay": "https://relay.example",
  "channels": [{
    "secret": "<채널 비밀 hex>",
    "members": [{ "label": "bob", "sign": "…", "kem": "…" }],
    "mentions": ["alice"]            // 이 이름이 불릴 때만 응답한다
  }]
}
```

**개인키와 채널 비밀이 들어 있다.** 권한이 `600` 보다 넓으면 어댑터가 읽지 않고 죽는다.

### 4. 에이전트에 등록한다

```bash
# Claude Code — 능동 주입
claude mcp add agent-channel-mesh -- bun run <repo>/src/adapter/bin.ts --delivery push

# Codex — 수신함 폴링
codex mcp add agent-channel-mesh -- bun run <repo>/src/adapter/bin.ts --delivery inbox
```

`--delivery` 는 필수다. 환경을 보고 추측하면 틀렸을 때 조용히 틀린다 — Claude 에서 수신함 모드로 뜨면 메시지가 도착해도 세션은 영원히 모르고, Codex 에서 주입 모드로 뜨면 선언한 capability 를 아무도 구현하지 않는다.

## 보호하는 것과 하지 않는 것

"종단 간 암호화"가 실제보다 많은 것을 약속하지 않도록 경계를 밝힌다.

**보호한다**
- 메시지 **내용**. 릴레이는 복호화할 수 없다.
- 발신자 유출에 대한 순방향 비밀성 — 메시지마다 새 임시 키를 쓴다.

**보호하지 않는다**
- **메타데이터.** 릴레이는 누가 누구에게, 언제, 얼마나 자주, 얼마나 크게 보내는지 본다. 소셜 그래프와 활동 패턴이 노출된다.
- **수신자 키 유출에 대한 순방향 비밀성.** HPKE(RFC 9180)는 어떤 모드에서도 이를 제공하지 않는다. 공격자가 장기 개인키를 훔치고 트래픽을 녹음해 뒀다면 전체 이력을 복호화한다. Double Ratchet 은 v2 검토 대상이다.

**릴레이의 상태**
릴레이는 **논리적으로 무상태**다 — 장기 프로세스를 돌리지 않고 세션·래칫·그룹 상태를 갖지 않는다. 다만 오프라인 전달을 위해 암호화된 blob 을 TTL 저장소에 보관한다. 그 내용은 읽을 수 없다.

## 요구 사항

- [Bun](https://bun.sh)
- Claude Code (채널 지원 버전) 또는 Codex (외부 MCP 서버 지원 버전)

## 라이선스

[Apache-2.0](LICENSE)
